# Plan 025: Partner preset and UI package parity (design spike)

## Status: SPIKE — partial (SDK READMEs added)

## Gap

Seven consumer presets vs two partner defaults (`partners.ts`). `partner-ui` documented but not published.

## Next steps

1. Product policy on default `OBLIVION_PARTNER_PRESETS`.
2. Publish `@oblivion/partner-ui` with semver CSS API.
3. Document env override in partner-api.md.